import 'package:flutter/material.dart';

class Carecentres extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("List of Care Centres Screen"),
        backgroundColor: Colors.green[600],
      ),
      body: Text("Care Centres list will be here"),
    );
  }
}
